# 持续集成环境搭建方案

## 📋 资源申请清单

### 1. 服务器资源（二选一）

#### 方案A：Linux服务器（推荐）
- **CPU**: 2核及以上
- **内存**: 4GB及以上（建议8GB）
- **硬盘**: 50GB及以上（SSD优先）
- **操作系统**: Ubuntu 20.04/22.04 LTS 或 CentOS 7/8
- **网络**: 公网IP（用于接收Webhook）或内网可访问
- **权限**: root或sudo权限

#### 方案B：Windows服务器
- **CPU**: 2核及以上
- **内存**: 4GB及以上（建议8GB）
- **硬盘**: 50GB及以上
- **操作系统**: Windows Server 2019/2022
- **网络**: 公网IP或内网可访问
- **权限**: 管理员权限

---

## 🛠️ 需要安装的软件清单

### 必需软件

1. **Jenkins**
   - 版本: 2.400+
   - 用途: CI/CD服务器
   - 安装方式: 通过包管理器或Docker

2. **Java JDK**
   - 版本: JDK 11 或 JDK 17
   - 用途: Jenkins运行环境 + Allure报告工具
   - 安装方式: OpenJDK或Oracle JDK

3. **Python**
   - 版本: Python 3.9+
   - 用途: 运行接口自动化测试
   - 安装方式: 系统包管理器或源码编译

4. **Git**
   - 版本: 2.0+
   - 用途: 代码拉取
   - 安装方式: 系统包管理器

5. **Allure命令行工具**
   - 版本: 2.24.0+
   - 用途: 生成测试报告
   - 安装方式: 下载二进制包或通过包管理器

### 可选软件（推荐）

6. **Docker**（可选）
   - 用途: 容器化部署，环境隔离
   - 优势: 便于管理和迁移

7. **Nginx**（可选）
   - 用途: 反向代理，访问Jenkins界面
   - 优势: 安全性和性能提升

---

## 📦 Jenkins插件清单

### 必需插件

1. **Git Plugin** - Git代码管理
2. **Pipeline Plugin** - Pipeline流水线支持
3. **Allure Jenkins Plugin** - Allure报告集成
4. **GitHub Plugin** 或 **GitLab Plugin** - 代码仓库集成（根据你的仓库选择）
5. **Credentials Plugin** - 凭证管理

### 推荐插件

6. **Blue Ocean** - 现代化Jenkins界面
7. **Timestamper** - 日志时间戳
8. **Workspace Cleanup** - 工作空间清理
9. **HTML Publisher** - HTML报告发布（备用）

---

## 🔧 系统配置要求

### 端口开放

- **8080**: Jenkins默认端口（可修改）
- **50000**: Jenkins Agent通信端口（如果使用分布式）
- **80/443**: Nginx端口（如果使用反向代理）

### 防火墙规则

- 允许Jenkins端口访问（8080）
- 允许Git仓库Webhook访问（如果使用Webhook触发）

### 网络要求

- 能够访问Git仓库（GitHub/GitLab）
- 能够访问测试环境API（173.249.27.176:8000）
- 能够访问钉钉API（发送通知）

---

## 📐 服务器架构建议

### 最小化部署（单机）

```
┌─────────────────────────────────┐
│      Linux/Windows 服务器        │
│  ┌──────────────────────────┐  │
│  │      Jenkins (8080)      │  │
│  │  - Git Plugin            │  │
│  │  - Allure Plugin         │  │
│  │  - Pipeline Plugin       │  │
│  └──────────────────────────┘  │
│  ┌──────────────────────────┐  │
│  │      Python 3.9+         │  │
│  │  - requests              │  │
│  │  - pytest                │  │
│  │  - allure-pytest         │  │
│  └──────────────────────────┘  │
│  ┌──────────────────────────┐  │
│  │      Java JDK 11         │  │
│  │  - Allure CLI            │  │
│  └──────────────────────────┘  │
└─────────────────────────────────┘
```

### 推荐部署（Docker）

```
┌─────────────────────────────────┐
│      Linux 服务器 (Docker)       │
│  ┌──────────────────────────┐  │
│  │   Jenkins Container       │  │
│  │   - 端口映射: 8080       │  │
│  │   - 数据卷: /var/jenkins │  │
│  └──────────────────────────┘  │
│  ┌──────────────────────────┐  │
│  │   Nginx (可选)            │  │
│  │   - 反向代理              │  │
│  │   - SSL证书              │  │
│  └──────────────────────────┘  │
└─────────────────────────────────┘
```

---

## 💾 存储空间估算

### 基础空间

- **Jenkins安装**: ~500MB
- **Java JDK**: ~300MB
- **Python环境**: ~200MB
- **Allure工具**: ~100MB
- **系统预留**: ~5GB

### 运行时空间

- **Jenkins工作空间**: 每次构建约100-500MB
- **测试报告**: 每次构建约10-50MB
- **构建历史**: 保留10次构建约1-5GB

**总计建议**: 至少50GB可用空间

---

## 🔐 安全配置要求

### 访问控制

- Jenkins管理员账号密码
- Git仓库访问凭证（如果需要私有仓库）
- 钉钉Webhook密钥（已在代码中配置）

### 网络安全

- 建议使用HTTPS访问Jenkins（通过Nginx反向代理）
- 配置防火墙规则，限制访问来源
- 定期更新系统和软件包

---

## 📝 申请资源时需要提供的信息

### 基本信息

1. **服务器用途**: 接口自动化测试CI/CD环境
2. **预计使用时长**: 长期使用
3. **访问方式**: SSH（Linux）或远程桌面（Windows）
4. **是否需要公网IP**: 是（用于Webhook接收）

### 技术规格

1. **操作系统**: Ubuntu 22.04 LTS（推荐）或 CentOS 8
2. **CPU**: 2核
3. **内存**: 4GB（最低），8GB（推荐）
4. **硬盘**: 50GB SSD
5. **网络**: 需要访问外网（GitHub/GitLab、钉钉API）

---

## 🚀 搭建步骤概览（供参考）

### 1. 服务器初始化
- 更新系统包
- 配置防火墙
- 创建Jenkins用户

### 2. 安装基础环境
- 安装Java JDK
- 安装Python 3.9+
- 安装Git
- 安装Allure命令行工具

### 3. 安装Jenkins
- 下载并安装Jenkins
- 配置Jenkins服务
- 初始化Jenkins（获取初始密码）

### 4. 配置Jenkins
- 安装必需插件
- 配置Git凭证
- 配置Allure工具路径
- 创建Pipeline任务

### 5. 配置自动触发
- 配置Git仓库Webhook
- 配置Jenkins触发条件
- 测试自动触发功能

### 6. 验证测试
- 手动触发一次构建
- 验证测试执行
- 验证报告生成
- 验证钉钉通知

---

## 📊 资源使用预估

### CPU使用率
- **空闲时**: <5%
- **构建时**: 20-50%（取决于测试用例数量）
- **峰值**: 70-80%（并发构建时）

### 内存使用
- **Jenkins服务**: ~500MB
- **单次构建**: ~500MB-1GB
- **总计**: 建议4GB以上

### 网络流量
- **代码拉取**: 每次约1-10MB
- **依赖安装**: 首次约50-100MB，后续增量更新
- **报告生成**: 每次约1-5MB

---

## ✅ 检查清单

申请资源前确认：

- [ ] 服务器规格满足最低要求
- [ ] 有root/sudo权限
- [ ] 可以访问外网（GitHub/GitLab、钉钉API）
- [ ] 可以访问测试环境API（173.249.27.176:8000）
- [ ] 端口8080可以开放（或使用其他端口）
- [ ] 有足够的存储空间（50GB+）

---

## 📞 后续支持

资源申请成功后，我可以协助：
1. 远程连接服务器进行环境搭建
2. 安装和配置所有必需软件
3. 配置Jenkins Pipeline
4. 配置自动触发机制
5. 测试和验证整个流程

---

## 💡 备选方案

如果暂时无法申请服务器，可以考虑：

1. **GitHub Actions**（如果代码在GitHub）
   - 免费额度：每月2000分钟
   - 无需服务器，开箱即用

2. **GitLab CI**（如果代码在GitLab）
   - 免费版支持CI/CD
   - 无需额外服务器

3. **继续使用本地Jenkins**
   - 配置自动触发
   - 需要保持电脑开机

---

**文档版本**: v1.0  
**最后更新**: 2026-02-03
